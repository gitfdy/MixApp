{
  "name": "MixApp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn react-native start",
    "bundle": "react-native bundle --platform android --entry-file index.js --bundle-output app/src/main/assets/index.android.bundle --dev false"
  },
  "dependencies": {
    "react": "^16.12.0",
    "react-native": "^0.62.3"
  }
}
